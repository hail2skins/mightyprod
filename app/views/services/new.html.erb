<%= provide(:title, "Add a service") %>
<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <h2>Add a service</h2>
    <%= render 'form' %><br />
    <p><%= link_to 'Back to main business page', owner_business_path(@owner, @business) %></p>
  </div>
</div>